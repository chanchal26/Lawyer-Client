import React from 'react';
import { Helmet } from "react-helmet";

const About = () => {
    return (
        <div>

            <h2>about</h2>
            <Helmet>
                <title>About</title>
            </Helmet>
        </div>
    );
};

export default About;